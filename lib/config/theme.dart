// Legacy/experimental file.
//
// This file previously contained a standalone demo `main()` + auction list.
// The real app entrypoint is `lib/main.dart` and routing is in
// `lib/core/router/app_router.dart`.
//
// Kept intentionally minimal to avoid analyzer errors.